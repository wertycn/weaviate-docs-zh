---
{}
---

要使用OIDC对Weaviate进行身份验证，您必须选择一个由身份提供者提供的流程，并创建特定于该流程的身份验证配置。

然后，Weaviate客户端将使用此配置进行身份验证。配置包括帮助客户端获取`访问令牌`的机密信息，以及（如果配置了）`刷新令牌`。

`访问令牌`会被添加到每个请求的HTTP头中，并用于与Weaviate进行身份验证。通常，这个令牌的有效期是有限的，当需要时可以使用`刷新令牌`来获取一组新的令牌。